spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/inventorydb
  data:
    redis:
      host: redis
  kafka:
    bootstrap-servers: kafka:29092
  rabbitmq:
    host: rabbitmq

grpc:
  server:
    port: 9083

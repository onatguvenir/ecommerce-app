spring:
  application:
    name: order-service

  # ---------------------------------------------------------------
  # Virtual Threads (Project Loom) — JDK 21 + Spring Boot 3.2+
  # Her HTTP isteği, @KafkaListener ve gRPC client çağrısı
  # virtual thread üzerinde koşar; Saga koordinasyonu için ideal.
  threads:
    virtual:
      enabled: true
  
  datasource:
    url: jdbc:postgresql://localhost:5432/orderdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
      properties:
        enable.idempotence: true
    consumer:
      group-id: order-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: '*'

# gRPC Clients
grpc:
  client:
    inventory-service:
      address: static://localhost:9083
      negotiationType: plaintext
    payment-service:
      address: static://localhost:9086
      negotiationType: plaintext
    user-service:
      address: static://localhost:9081
      negotiationType: plaintext

# Server Configuration
server:
  port: 8085
  shutdown: graceful

# Management & Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans

# Logging
logging:
  level:
    root: INFO
    com.monat.ecommerce: DEBUG

# Resilience4j
resilience4j:
  circuitbreaker:
    instances:
      inventoryService:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
      paymentService:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
  
  retry:
    instances:
      inventoryService:
        maxAttempts: 3
        waitDuration: 1000ms
      paymentService:
        maxAttempts: 3
        waitDuration: 1000ms
        
  bulkhead:
    instances:
      inventory-service:
        maxConcurrentCalls: 50
        maxWaitDuration: 500ms
      payment-service:
        maxConcurrentCalls: 50
        maxWaitDuration: 500ms
      user-service:
        maxConcurrentCalls: 100
        maxWaitDuration: 500ms


# Application specific
application:
  saga:
    timeout-minutes: 15
  outbox:
    polling-interval-ms: 5000
    batch-size: 100
